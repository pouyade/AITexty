# Copy to keystore.properties and fill in for Play Store / production signing.
# Without this file, release builds are signed with the debug keystore (fine for sideloading).
storeFile=release.keystore
storePassword=your-store-password
keyAlias=your-key-alias
keyPassword=your-key-password
